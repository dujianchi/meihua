import 'package:flutter/material.dart';

class YiJing extends StatelessWidget {
  const YiJing({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('易经原文'),
      ),
      body: const Center(
        child: Text('易经原文'),
      ),
    );
  }
}
